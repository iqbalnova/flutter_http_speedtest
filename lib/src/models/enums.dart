// lib/src/models/enums.dart

enum TestPhase { metadata, ping, download, upload }

enum PhaseState { success, failed, timeout, canceled }

enum NetworkQualityGrade { bad, poor, average, good, great }
